# 자산관리 대시보드 멀티유저 전환 설계

**작성일**: 2026-05-26
**스코프**: 1인 localStorage 기반 대시보드를 가족·친구(5~30명) 규모의 멀티유저 웹앱으로 전환
**향후 확장**: 일반 공개·SaaS 단계는 별도 spec/plan에서 다룰 예정 (본 문서 §11 "스코프 외" 참고)

---

## 1. 결정 사항 (Decisions)

| 항목 | 결정 |
|---|---|
| 타깃 사용자 | 가족·친구 5~30명, 초대제 (화이트리스트) |
| 앱 형태 | 웹앱 + PWA 설치 지원 (iOS / Android / 데스크탑) |
| 인증 | Supabase Auth, Google + 카카오 OAuth |
| 데이터 저장 | Supabase Postgres (RLS로 사용자별 격리) |
| 기존 localStorage 데이터 | 폐기, 새 계정으로 시작 |
| OpenAI 비용 | 호스트 전액 부담 + 사용자별 일일 호출 제한 (기본 20회) |
| 백엔드 인프라 | Supabase(auth+DB) + 기존 Express(OpenAI/Yahoo 프록시) 병행 |
| 호스팅 | Vercel(클라이언트) + Railway(Express) + Supabase Cloud |

---

## 2. 전체 아키텍처

```
[브라우저]                  [Vercel]                    [Supabase Cloud]
React + Vite      ─────►    정적 호스팅                 auth.users
supabase-js                                             public.holdings
       │                                                public.transactions
       │ (1) OAuth → JWT 발급                           public.user_settings
       │                                                public.ai_usage
       │                                                public.allowed_emails
       │                                                RLS + 트리거
       │
       ├─ (2) supabase-js로 자기 데이터 CRUD  ────────►  Postgres
       │
       └─ (3) AI / 시세 호출 + Authorization: Bearer
                              │
                              ▼
                      [Railway]
                      Express 서버
                      • JWT 검증 (로컬 HS256, jose)
                      • zod 입력 검증
                      • Rate limit (Postgres ai_usage)
                      • Yahoo / 뉴스 / OpenAI 프록시
                      • OPENAI_API_KEY, SERVICE_ROLE_KEY 보관
```

**핵심 원칙**
1. 보유 종목·거래 등 사용자 데이터는 클라이언트가 supabase-js로 직접 CRUD. RLS가 격리.
2. 외부 API 프록시는 기존 Express 그대로. 라우트 앞단에 JWT 검증 + rate limit만 추가.
3. 민감 키(OpenAI API key, Supabase service_role key)는 서버 환경변수에만.

---

## 3. 데이터 모델

```sql
create table public.holdings (
  id          bigint generated by default as identity primary key,
  user_id     uuid not null references auth.users(id) on delete cascade,
  category    text not null check (category in ('kr','us','crypto')),
  symbol      text not null,
  name        text not null,
  created_at  timestamptz not null default now(),
  unique (user_id, symbol)
);
create index on public.holdings (user_id);

create table public.transactions (
  id          bigint generated by default as identity primary key,
  user_id     uuid not null references auth.users(id) on delete cascade,
  holding_id  bigint not null references public.holdings(id) on delete cascade,
  type        text not null check (type in ('buy','sell')),
  quantity    numeric not null check (quantity > 0),
  price       numeric not null check (price >= 0),
  date        date not null,
  fee         numeric not null default 0 check (fee >= 0),
  created_at  timestamptz not null default now()
);
create index on public.transactions (user_id);
create index on public.transactions (holding_id);

create table public.user_settings (
  user_id     uuid primary key references auth.users(id) on delete cascade,
  target      jsonb not null default '{"kr":30,"us":50,"crypto":20}',
  fx_rate     numeric not null default 1380 check (fx_rate > 0),
  updated_at  timestamptz not null default now()
);

create table public.ai_usage (
  user_id     uuid not null references auth.users(id) on delete cascade,
  usage_date  date not null,
  count       integer not null default 0,
  primary key (user_id, usage_date)
);

create table public.allowed_emails (
  email       text primary key,
  invited_by  uuid references auth.users(id),
  created_at  timestamptz default now()
);
```

**선택 근거**
- `id`는 bigint identity — 클라이언트의 숫자 id와 호환
- 종목·거래는 list 단위로 자주 조회 → `user_id` 인덱스 필수
- 종목 캐시(시세·차트)는 DB에 안 넣음. 서버 `node-cache`에 사용자 무관 공용 캐시 유지 (효율 ↑)

---

## 4. 보안 강화 RLS 정책

### 4.1 RLS 강제 활성화

```sql
alter table public.holdings, public.transactions,
            public.user_settings, public.ai_usage, public.allowed_emails
  enable row level security;
alter table public.holdings, public.transactions,
            public.user_settings, public.ai_usage, public.allowed_emails
  force row level security;   -- ★ postgres role bypass 차단
```

### 4.2 권한 최소화

```sql
revoke all on public.holdings, public.transactions,
              public.user_settings, public.ai_usage, public.allowed_emails
  from public, anon;
grant select, insert, update, delete
  on public.holdings, public.transactions, public.user_settings
  to authenticated;
grant select on public.ai_usage to authenticated;
-- allowed_emails는 service_role 외 접근 불가
```

### 4.3 정책 (동작별 분리)

```sql
-- holdings
create policy holdings_select on public.holdings
  for select to authenticated using (user_id = auth.uid());
create policy holdings_insert on public.holdings
  for insert to authenticated with check (user_id = auth.uid());
create policy holdings_update on public.holdings
  for update to authenticated
  using       (user_id = auth.uid())
  with check  (user_id = auth.uid());
create policy holdings_delete on public.holdings
  for delete to authenticated using (user_id = auth.uid());

-- transactions: 본인 + holding 소유권까지 검증
create policy tx_select on public.transactions
  for select to authenticated using (user_id = auth.uid());
create policy tx_insert on public.transactions
  for insert to authenticated with check (
    user_id = auth.uid()
    and exists (
      select 1 from public.holdings h
      where h.id = holding_id and h.user_id = auth.uid()
    )
  );
create policy tx_update on public.transactions
  for update to authenticated
  using       (user_id = auth.uid())
  with check  (
    user_id = auth.uid()
    and exists (
      select 1 from public.holdings h
      where h.id = holding_id and h.user_id = auth.uid()
    )
  );
create policy tx_delete on public.transactions
  for delete to authenticated using (user_id = auth.uid());

-- user_settings: DELETE 정책 의도적 미작성 → 차단
create policy us_select on public.user_settings
  for select to authenticated using (user_id = auth.uid());
create policy us_insert on public.user_settings
  for insert to authenticated with check (user_id = auth.uid());
create policy us_update on public.user_settings
  for update to authenticated
  using (user_id = auth.uid()) with check (user_id = auth.uid());

-- ai_usage: 사용자는 SELECT만, 갱신은 service_role
create policy au_select on public.ai_usage
  for select to authenticated using (user_id = auth.uid());
-- INSERT/UPDATE/DELETE 정책 의도적 미작성
```

### 4.4 트리거 (위변조 차단)

```sql
-- INSERT 시 user_id·created_at 서버 강제 주입
create function public.set_user_id_from_jwt()
returns trigger language plpgsql security invoker as $$
begin
  new.user_id := auth.uid();
  new.created_at := now();
  return new;
end $$;

create trigger holdings_set_user_id
  before insert on public.holdings
  for each row execute function public.set_user_id_from_jwt();
-- transactions, user_settings도 동일

-- UPDATE 시 user_id / created_at 변경 차단
create function public.prevent_immutable_changes()
returns trigger language plpgsql security invoker as $$
begin
  new.user_id    := old.user_id;
  new.created_at := old.created_at;
  return new;
end $$;
-- BEFORE UPDATE 트리거 연결
```

### 4.5 가입 화이트리스트

```sql
create function public.enforce_invite_only()
returns trigger language plpgsql security definer
set search_path = public, auth as $$
begin
  if not exists (select 1 from public.allowed_emails where email = new.email) then
    raise exception 'Email not in invite list: %', new.email;
  end if;
  return new;
end $$;

create trigger users_invite_check
  before insert on auth.users
  for each row execute function public.enforce_invite_only();
```

### 4.6 운영 보안

| 항목 | 조치 |
|---|---|
| `anon` key | 클라이언트 노출 OK. anon은 어떤 테이블에도 권한 없음 |
| `service_role` key | 서버 `.env`에만. 클라이언트 번들 grep 검증 단계에 포함 |
| Auth providers | Supabase 대시보드에서 Google·Kakao만 enable. email 자체가입 disable |
| JWT 만료 | access 1h, refresh 7일 (Supabase 기본값 유지) |
| CORS | `https://<클라이언트 도메인>`만 origin 허용 |
| CSP | `default-src 'self'; connect-src 'self' https://*.supabase.co https://api.openai.com` |
| 로그 | request log에 user_id만. 이메일·토큰 금지 |

### 4.7 보안 검증 체크리스트

구현 후 `scripts/security-smoke.js`로 자동 실행:

1. A의 JWT로 SELECT → A 데이터만 반환
2. A가 `user_id = B`로 INSERT 시도 → 거부 (트리거가 덮어쓰거나 RLS 거부)
3. A가 UPDATE로 `user_id` 변경 시도 → 트리거가 복원, RLS도 거부
4. A가 B의 holding_id로 transaction INSERT → tx_insert 정책 EXISTS 절에서 거부
5. 사용자가 `update ai_usage set count = 0` → 정책 없어서 거부
6. 화이트리스트에 없는 이메일로 OAuth 로그인 → trigger raise exception
7. anon key로 모든 테이블 SELECT → 0 rows (권한 없음)
8. 클라이언트 빌드 산출물(`dist/**/*.js`) grep → `service_role` 키 / `JWT_SECRET` 미포함

---

## 5. 인증 흐름

```
사용자                  클라이언트                Supabase            Express
────                    ────                      ────                ────
로그인 페이지 진입
"Google로 시작" 클릭   signInWithOAuth(google)
                       ─────────────────────────►
                                                  Google consent
                                                  → 콜백, 세션 발급
                                                  (화이트리스트 trigger
                                                   통과 시에만)
                       ◄─────────────────────────
                       (access JWT + refresh,
                        supabase-js가 localStorage에 보관)

대시보드 진입          listHoldings() 등
                       ─────────────────────────►  RLS 통과한 행만 반환
                       ◄─────────────────────────

AI 호출                fetch('/api/news', {
                         Authorization: Bearer <jwt>
                       })                                              ─────► authMiddleware
                                                                              ↓ HS256 검증
                                                                              ↓ req.user 주입
                                                                              ↓ zod 검증
                                                                              ↓ chargeAiUsage
                                                                                (캐시 hit면 skip)
                                                                              ↓ 기존 핸들러
                       ◄────────────────────────────────────────────────  결과 JSON

JWT 만료(1h)           supabase-js가 refresh로 자동 재발급
로그아웃               supabase.auth.signOut()
```

---

## 6. 서버 변경 (Express)

### 6.1 미들웨어 스택

```js
app.use(helmet())
app.use(cors({ origin: ALLOWED_ORIGINS, credentials: false }))
app.use(express.json({ limit: '10kb' }))
app.use(ipRateLimit({ windowMs: 60_000, max: 60 }))   // anon DoS 방지
app.use('/api', authMiddleware)
// 라우트:
app.get ('/api/quote/:symbol',           quoteHandler)
app.post('/api/news',                    newsHandler)         // AI 한도는 핸들러에서 charge
app.post('/api/stock/:symbol/analysis',  stockAnalysisHandler)
```

### 6.2 JWT 검증 (`server/middleware/auth.js`)

로컬 HS256 검증 (`jose`). 만료 즉시 거부, revocation은 1h 만료에 의존 (가족·친구 규모 정책).

```js
import { jwtVerify } from 'jose';
const SECRET = new TextEncoder().encode(process.env.SUPABASE_JWT_SECRET);

export async function authMiddleware(req, res, next) {
  const h = req.headers.authorization;
  if (!h?.startsWith('Bearer ')) return res.status(401).json({ error: 'no_token' });
  try {
    const { payload } = await jwtVerify(h.slice(7), SECRET, {
      issuer: `${process.env.SUPABASE_URL}/auth/v1`,
      algorithms: ['HS256'],
    });
    req.user = { id: payload.sub, email: payload.email };
    next();
  } catch {
    res.status(401).json({ error: 'invalid_token' });
  }
}
```

### 6.3 Rate Limit / 사용량 (`server/lib/usage.js`)

캐시 hit은 카운트 안 올림. OpenAI 호출 직전에만 charge.

```js
export async function chargeAiUsage(userId, dailyLimit = 20) {
  const today = todayUTC();
  const { data, error } = await supabaseAdmin.rpc('increment_ai_usage', {
    p_user_id: userId, p_date: today,
  });
  if (error) throw new Error('usage_error');
  if (data.count > dailyLimit) {
    throw new RateLimitError({ used: data.count, limit: dailyLimit, resetAt: `${today}T23:59:59Z` });
  }
  return data.count;
}
```

Postgres atomic 함수:

```sql
create function public.increment_ai_usage(p_user_id uuid, p_date date)
returns table(count int)
language plpgsql security definer set search_path = public as $$
begin
  return query
  insert into public.ai_usage (user_id, usage_date, count)
    values (p_user_id, p_date, 1)
    on conflict (user_id, usage_date)
    do update set count = ai_usage.count + 1
    returning ai_usage.count;
end $$;
```

기존 `analyze.js` 변경:

```js
export async function analyzeStock(symbol, userId) {
  const cached = cache.get(`stock-ai:${symbol}`);
  if (cached) return cached;
  await chargeAiUsage(userId);
  const result = await openai.chat.completions.create(...);
  cache.set(`stock-ai:${symbol}`, result);
  return result;
}
```

### 6.4 입력 검증 (`server/validators.js`)

```js
import { z } from 'zod';
export const StockSymbolSchema = z.string().regex(/^[A-Z0-9.\-]{1,15}$/);
export const NewsBodySchema = z.object({
  holdings: z.array(z.object({
    symbol: StockSymbolSchema, name: z.string().max(50),
  })).max(50),
});
```

### 6.5 에러 응답 통일

```json
{ "error": "<code>", "message": "<korean>", "details": {} }
```
코드: `no_token` `invalid_token` `rate_limit` `validation_error` `upstream_error` `usage_error`

### 6.6 환경변수

```
PORT=3001
OPENAI_API_KEY=sk-...
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_JWT_SECRET=<dashboard에서 복사>
SUPABASE_SERVICE_ROLE_KEY=eyJ...
ALLOWED_ORIGINS=https://<도메인>,http://localhost:5173
DAILY_AI_LIMIT=20
```

---

## 7. 클라이언트 변경 (React)

### 7.1 신규 모듈

```
client/src/
  lib/
    supabase.js              # createClient 싱글톤
    api.js                   # Express 호출 헬퍼 (JWT 자동 첨부)
    db/holdings.js           # listHoldings / addHolding / removeHolding
    db/transactions.js       # list / add / update / delete
    db/userSettings.js       # get / updateTarget / updateFxRate
    useRemoteState.js        # useHoldings / useTransactions / useSettings
  AuthProvider.jsx           # 세션 구독 + context 제공
  AuthGate.jsx               # 세션 유무에 따라 LoginPage / AssetDashboard
  LoginPage.jsx              # Google·Kakao 버튼
```

### 7.2 `useLocalStorage` → `useRemoteState`

호출부 시그니처 최대한 유사하게 유지해 `AssetDashboard.jsx` 변경 최소화:

```js
// before
const [holdingsRaw, setHoldingsRaw] = useLocalStorage("holdings", SAMPLE_HOLDINGS);
// after
const { holdings: holdingsRaw, add: addHolding, remove: removeHolding, loading } = useHoldings();
```

### 7.3 Express 호출 래퍼 (`lib/api.js`)

```js
export async function apiPost(path, body) {
  const { data: { session } } = await supabase.auth.getSession();
  if (!session) throw new Error('Not authenticated');
  const res = await fetch(import.meta.env.VITE_API_BASE + path, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${session.access_token}`,
    },
    body: JSON.stringify(body),
  });
  if (res.status === 429) throw new RateLimitError(await res.json());
  if (!res.ok) throw new Error(await res.text());
  return res.json();
}
```

기존 `fetch('/api/news', ...)` 호출 4~5개를 `apiPost`로 교체.

### 7.4 PWA

- `vite-plugin-pwa` 추가 (auto update, 오프라인 캐시는 정적 자산만)
- `manifest.webmanifest`: 이름·아이콘·테마색
- 데이터는 항상 네트워크 fetch (오프라인 데이터 sync는 스코프 외)

### 7.5 환경변수

```
client/.env.local
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
VITE_API_BASE=http://localhost:3001            # dev
# prod: https://api.<도메인>
```

`VITE_` 미접두 변수는 클라이언트 번들에 절대 안 들어감 → service_role 키 누출 사고 차단.

### 7.6 UX

| 상황 | 동작 |
|---|---|
| 세션 로딩 | AuthGate 스피너 |
| 데이터 로딩 | 카드 skeleton |
| Supabase 에러 | 토스트 + 재시도 버튼 |
| 429 rate limit | 모달: "오늘 AI 분석 한도(20회) 초과. 내일 다시 사용 가능" |
| JWT 만료 후 refresh 실패 | 강제 로그아웃 → LoginPage |
| 화이트리스트 거부 | LoginPage에 안내: "초대받은 분만 사용 가능. 호스트에게 문의" |

### 7.7 제거 / 정리

- `useLocalStorage` 훅, `STORAGE_PREFIX`, `SAMPLE_HOLDINGS`, `SAMPLE_TRANSACTIONS` 제거
- "전체 초기화" 버튼 → DB DELETE로 동작 변경

---

## 8. 배포 토폴로지

| 컴포넌트 | 호스팅 | 비용 |
|---|---|---|
| 클라이언트 | Vercel Hobby | $0 |
| Express | Railway Hobby | $5/월 (always-on) |
| Postgres + Auth | Supabase Free | $0 |
| 도메인 | 가비아·Cloudflare | ~$15/년 |
| OpenAI (30명) | | $2~7/월 |
| **합계** | | **약 $5/월 + 도메인 + AI 사용량** |

### DNS

```
dashboard.<도메인>  → Vercel
api.<도메인>        → Railway
```

---

## 9. 테스트 전략

기존 방침 유지: 순수 함수만 node:test, UI·라우트·RLS는 통합 검증.

| 영역 | 방법 |
|---|---|
| 순수 함수 | `node --test` — `lib/usage.js`, `validators.js`, 기존 `computeStats` 등 |
| JWT 미들웨어 | 유효/만료/위변조 3 케이스 통합 테스트 |
| Rate limit | 20회 통과, 21회째 429 통합 테스트 |
| RLS 보안 | `scripts/security-smoke.js`로 §4.7 8항목 자동 실행 |
| E2E | Playwright — 로그인 → 종목 추가 → AI 분석 happy path 1개 + 화이트리스트 거부 1개 |

---

## 10. 롤아웃 단계

```
Phase 0: 인프라 (1일)
  - Supabase 프로젝트, Google·Kakao OAuth 등록
  - Railway·Vercel 프로젝트, 도메인, secrets

Phase 1: DB + 보안 (1일)
  - 스키마·RLS·트리거·함수 SQL 적용
  - 화이트리스트에 본인 이메일만
  - security-smoke 8항목 통과 확인

Phase 2: 서버 (1일)
  - auth 미들웨어, usage 헬퍼, zod, helmet, ipRateLimit
  - 기존 라우트에 부착
  - 통합 테스트

Phase 3: 클라이언트 (1~2일)
  - supabase / db 모듈 / useRemoteState
  - AuthGate, LoginPage, AuthProvider
  - AssetDashboard에서 useLocalStorage 교체
  - api.js로 fetch 호출 교체
  - PWA 설정

Phase 4: 본인 dogfooding (1~3일)
  - 본인 계정으로 실데이터 운영
  - 버그·UX 수정
  - 비용 추적해 DAILY_AI_LIMIT 재조정

Phase 5: 가족·친구 초대 (점진)
  - 1명씩 화이트리스트 추가, 안내
  - 피드백 받으며 확장
```

---

## 11. 스코프 외 (1차 출시 제외)

**기능**
- 가족 자산 합산 보기, 공유, 협업
- 푸시 알림
- 다국어 (한국어만)
- 광고·결제·이용약관·개인정보처리방침
- 모바일 네이티브 앱 (PWA로 충분)
- 계정 삭제·이메일 변경 셀프서비스 (호스트가 Supabase Dashboard에서)

**보안** (확장 시 재검토 — `memory/project_multi_user_security_deferred.md`에 기록)
- 2FA, audit log, 키 자동 회전, WAF, pgaudit, PITR, Sentry/APM

---

## 12. 알려진 위험 / 의사결정 근거

| 항목 | 위험 | 결정 |
|---|---|---|
| 로컬 JWT 검증 (revocation 1h 지연) | BAN 즉시 차단 불가 | 가족·친구 규모에서 BAN 사례 희박. 만료(1h) + 화이트리스트 삭제로 충분. 확장 시 `supabase.auth.getUser` 호출로 전환 검토 |
| Supabase vendor lock-in | 다른 BaaS로 마이그레이션 어려움 | Postgres 기반이라 DB는 그대로 옮길 수 있음. Auth만 재작성 (NextAuth 등) |
| Free tier 한도 (DB 500MB, MAU 50k) | 가족·친구 규모는 여유. 확장 시 $25/월 Pro | 확장 시점에 평가 |
| Railway Hobby ($5/월) cold start 없음 | 신뢰성 양호 | Render Free의 cold start 대비 가족 첫 진입 경험 우선 |
| 기존 localStorage 데이터 폐기 | 본인 입력 데이터 손실 | 사용자 명시 동의. 새 계정으로 재입력 |

---

## 13. 다음 단계

이 spec 승인 후 `writing-plans` skill로 Phase 0~5 각각의 bite-sized task 분해 + 코드 블록 포함된 구현 계획을 작성한다. 위치: `docs/superpowers/plans/2026-05-26-multi-user-rollout.md`.
